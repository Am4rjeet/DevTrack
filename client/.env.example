# API base URL — required in production (Vercel + Render)
# Dev: leave unset to use Vite proxy at /api/v1
# VITE_API_URL=https://your-api.onrender.com/api/v1

# Optional: Cloudflare Turnstile site key for bot protection on auth forms
# VITE_TURNSTILE_SITE_KEY=
